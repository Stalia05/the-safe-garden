const button = document.getElementById("validate");
const message = document.getElementById("message");

button.addEventListener("click", () => {
  message.textContent =
    "Bien joué 🌿 Tu es ici, maintenant. Prends une respiration.";
});
